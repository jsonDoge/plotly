import * as anchor from '@coral-xyz/anchor'

export const setupFarm = async (program, payer): Promise<void> => {
  try {
    // TODO any necessary setup?
  } catch (error) {
    console.error('Error farm setup:', error)
    throw error
  }
}
